apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: oidc-auth
  namespace: kube-system
  annotations:
    kubernetes.io/ingress.class: traefik
spec:
  rules:
  - http:
      paths:
      - path: /
        backend:
          serviceName: oidc-auth
          servicePort: 80
